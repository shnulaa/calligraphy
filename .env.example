# API 配置
# 支持多个 API Key，用逗号分隔，系统会自动轮询使用
# 例如: API_KEY=key1,key2,key3 需要模型支持视觉
API_KEY=key1,key2,key3
API_URL=https://generativelanguage.googleapis.com
MODEL=gemini-2.5-flash

# 网络配置说明：
# 如果遇到 TLS 连接错误 (Client network socket disconnected)，可能的解决方案：
# 1. 检查网络代理设置
# 2. 确保防火墙允许访问 generativelanguage.googleapis.com
# 3. 如果在中国大陆，可能需要配置代理或使用 VPN
# 4. 检查系统时间是否正确（TLS 证书验证需要）
# 5. 尝试重启网络或更换网络环境

# 端口配置（内部固定，无需修改）
# 前端: 33000
# 后端: 33001
# Nginx对外: 48888
